cat /dev/random | head -n 1 | cat -e - зависает, выдает результат только после кнтрл-с
env puis export puis env # vars aren't sorted
cat | cat | cat | ls
export ; env # display is different for both commands
cat < Makefile | grep gcc > output
